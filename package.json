{
  "name": "arenamind-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install && npm run install:backend && npm run install:frontend",
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "start": "node start-dev.js",
    "test:smoke": "node scripts/test-smoke.js",
    "test:frontend": "node scripts/test-frontend-sanity.js",
    "test": "npm run test:frontend && npm run test:smoke"
  }
}
